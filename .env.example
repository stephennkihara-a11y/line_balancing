# Copy to .env and fill in. Loaded by docker-compose.

# JWT signing secret (change for production, must be >= 32 chars)
JWT_SECRET=change-me-in-production-please-32-chars-min

# Anthropic Claude API key — required for the AI explanation layer
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
